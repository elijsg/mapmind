import React from "react";

const Header: React.FC = () => {
  return (
    <header className={`p-4`}>
      <h1 className={`text-4xl font-bold`}>MindMap</h1>
    </header>
  );
};

export default Header;

